# 엑셀 컬럼명이 바뀌었을 때 수정하는 방법

> 📌 **이 문서는 누구를 위한 것?**
> 새 엑셀 파일을 받았는데 컬럼명이 예전과 달라서 코드를 고쳐야 할 때, **프로그래밍을 잘 모르는 분도 따라 할 수 있도록** 작성된 안내서입니다.

---

## 🔍 먼저 알아야 할 것

`main.py` 코드 안에는 **엑셀 컬럼명**을 작은따옴표(`'...'`)로 감싸서 적어둔 곳이 있습니다.
엑셀 파일에서 컬럼명이 바뀌면, **코드 안의 따옴표 안 글자만** 바꿔주면 됩니다.

⚠️ **사이트(웹페이지)의 ID는 절대 건드리지 마세요.**
사이트는 우리가 바꿀 수 없습니다. 우리가 바꾸는 건 **엑셀 컬럼명을 적어둔 부분**뿐입니다.

---

## ✏️ 수정해야 하는 곳은 총 4군데

`main.py` 파일을 메모장이나 VS Code로 열어주세요.

### ① 환자 ID 컬럼 (1번째 위치) — 약 136번째 줄

```python
p_id = str(row.get('연구내원번호', index + 1))
```

여기서 `'연구내원번호'` 부분을 새 엑셀의 환자 식별 컬럼명으로 바꿉니다.

### ② 환자 ID 컬럼 (2번째 위치) — 약 235번째 줄

```python
p_id = str(df.loc[idx].get('연구내원번호', idx + 1))
```

여기도 똑같이 `'연구내원번호'`를 바꿉니다.

> ⚠️ **①과 ②는 항상 같은 값**으로 바꿔야 합니다.
> 예를 들어 환자번호 컬럼이 `'PatientID'`로 바뀌었다면, 두 곳 모두 `'PatientID'`로 바꿔야 함.

### ③ 수치 데이터 17개 매핑 — 약 142~160번째 줄

```python
mapping = {
    "Bilirubin":              row.get('Bilirubin'),
    "Creatinine":             row.get('Creatinine'),
    "Albumin":                row.get('Albumin'),
    "Glucose":                row.get('Glucose'),
    "Sodium":                 row.get('Na'),
    "Hematocrit":             row.get('Hematocrit'),
    "White blood cell count": row.get('WBC'),
    "Platelet":               row.get('Platelets'),
    "PaCO2":                  row.get('PaCO2'),
    "PaO2/FiO2 ratio":        row.get('PaO/FiO ratio'),
    "Temperature":            row.get('BT'),
    "Respiratory rate":       row.get('Respiratory rate'),
    "Heart rate":             row.get('Heart rate'),
    "Systolic blood pressure":row.get('Systolic blood pressure'),
    "Urine output":           row.get('Urine output'),
    "Minute Ventilation":     row.get('Minute Ventilation'),
    "Bicarbonate":            row.get('Bicarbonate'),
}
```

각 줄은 다음 구조입니다:

```
"왼쪽":                       row.get('오른쪽'),
 │                                    │
 └─ 사이트의 입력칸 ID                  └─ 엑셀 컬럼명
    (절대 건드리지 마세요)               (이 부분만 바꿉니다)
```

**예시:** 엑셀에서 `'Respiratory rate'`가 `'RR'`로 바뀌었다면:

```python
"Respiratory rate":       row.get('RR'),
```

→ **왼쪽은 그대로**, 오른쪽 `'Respiratory rate'`만 `'RR'`로 변경.

### ④ Vasopressors 컬럼 — 약 168번째 줄

```python
v_col = 'Vasopressors (Use of vasopressors?)'
```

여기서 `'Vasopressors (Use of vasopressors?)'`를 새 엑셀의 vasopressor 사용 여부 컬럼명으로 바꿉니다.

예: 새 엑셀에서 `'vasopression 사용여부'`로 바뀌었다면:
```python
v_col = 'vasopression 사용여부'
```

---

## 📋 한눈에 보는 체크리스트

엑셀 컬럼명이 바뀌었을 때, 아래 4가지를 모두 확인하세요:

- [ ] **136번째 줄** — 환자 ID 컬럼명
- [ ] **142~160번째 줄** — 17개 수치 컬럼명 (`row.get(...)` 부분만)
- [ ] **168번째 줄** — Vasopressors 컬럼명
- [ ] **235번째 줄** — 환자 ID 컬럼명 (136번째와 같은 값)

---

## ❓ 자주 묻는 질문

### Q1. 엑셀에 컬럼이 아예 없으면 어떻게 되나요?
A. 자동으로 건너뜁니다. 에러가 나지 않고, 그 항목은 빈칸으로 사이트에 전송됩니다.
예를 들어 `'Minute Ventilation'` 컬럼이 엑셀에 없으면, 사이트의 Minute Ventilation 입력칸은 빈 상태로 유지됩니다.

### Q2. 컬럼명에 띄어쓰기가 있어도 되나요?
A. 네, 됩니다. `'PaO/FiO ratio'`처럼 띄어쓰기/특수문자가 있어도 따옴표 안에 그대로 적으면 됩니다.

### Q3. 컬럼명 앞뒤에 공백(스페이스)이 있으면 어떻게 되나요?
A. 코드가 자동으로 공백을 제거해줍니다. 신경 안 써도 됩니다.

### Q4. 대소문자를 구분하나요?
A. **네, 구분합니다.** 엑셀이 `'WBC'`인데 코드에 `'wbc'`라고 쓰면 못 찾습니다. **정확히 일치**해야 합니다.

### Q5. Vasopressors 값은 어떻게 적혀 있어야 하나요?
A. 엑셀 셀에 **`Yes` 또는 `No`** 라고 적혀 있어야 사이트에서 선택됩니다.
- `0` / `1`, `사용` / `미사용`, `Y` / `N` 등은 인식하지 못합니다.
- 비어있거나 다른 값이면 자동으로 `'No'`로 처리됩니다.

### Q6. 단위(mg/dL, mmHg 등)도 같이 적나요?
A. **아니요.** 코드에는 컬럼명만 적습니다. 사이트가 기대하는 단위로 엑셀에 미리 값이 입력되어 있어야 합니다.

### Q7. 사이트 ID(왼쪽 `"Bilirubin"` 부분)는 왜 건드리면 안 되나요?
A. 그건 우리 코드가 아니라 **사이트 자체의 HTML 구조**이기 때문입니다.
사이트 운영자가 페이지를 바꾸지 않는 한 그대로 둬야 합니다.

---

## 🧪 수정 후 확인하는 방법

1. `main.py`를 저장합니다.
2. 명령 프롬프트(또는 PowerShell)에서 다음 명령을 실행:
   ```bash
   uv run main.py
   ```
3. 엑셀 파일 선택창이 뜨면 새 파일을 선택합니다.
4. 처리하고 싶은 행 범위를 입력합니다 (예: 시작 1, 끝 5 → 5명만 테스트).
5. 콘솔에 다음과 같은 에러가 안 뜨면 성공:
   - `⚠ '필드이름' 입력 실패` (모든 환자에서 똑같이 뜨면 컬럼명 매칭 실패 가능성)
6. 결과 엑셀 파일(`RESULT_...xlsx`)을 열어 `Probability_Result` 컬럼에 `%` 값이 채워졌는지 확인합니다.

---

## 📞 그래도 안 될 때

- 모든 환자에서 결과가 `N/A`로 나온다면 → 컬럼명 매칭 또는 사이트 접속 문제
- 특정 컬럼만 값이 안 들어간다면 → 그 컬럼의 매핑이 잘못된 것
- 콘솔에 빨간 에러가 떴다면 → 캡처해서 도움 요청

---

# 📁 프로그램이 만드는 파일들 — 헷갈리지 않게 정리

프로그램을 돌리면 폴더에 여러 가지 엑셀 파일이 생깁니다. 이름이 비슷해서 헷갈릴 수 있으니, **어떤 파일이 언제 만들어지고, 어떻게 처리해야 하는지** 정리합니다.

## 만들어지는 파일 3종류

| 파일 이름 (앞부분) | 언제 만들어짐 | 무엇이 들어 있음 | 보관 여부 |
|---|---|---|---|
| `BACKUP_...xlsx` | 한 명 처리할 때마다 (계속 덮어씀) | 지금까지 처리한 결과 (중간 저장) | **자동 삭제됨** (작업 정상 완료 시) |
| `RESULT_...xlsx` | 작업이 끝까지 완료된 후 1번 | **최종 결과** (전체 데이터 + 예측값) | ✅ **보관** |
| `NA_...xlsx` | 결과를 못 가져온 환자가 있을 때만 | N/A로 표시된 환자들만 따로 모음 | ✅ **보관** (재처리 용도) |

---

## 1️⃣ BACKUP 파일 — 가장 헷갈리는 파일

### 파일 이름 예시
```
BACKUP_강남septic shock_대상자_0511_수집중_1_1495.xlsx
       └─ 원본 엑셀 이름 ───────────────────┘ └┬┘ └┬─┘
                                            시작행 끝행
```

### 언제 만들어지나요?
- 작업을 시작하면 **첫 환자 처리 직후**에 만들어집니다.
- 그 후 **한 명을 처리할 때마다** 같은 파일에 덮어쓰기로 업데이트됩니다.
- 즉, 작업 중간에 프로그램이 꺼져도 **지금까지 한 결과는 이 파일 안에 안전하게 저장**되어 있습니다.

### 이 파일은 왜 존재하나요?
**작업을 이어서 할 수 있게 해주는 "세이브 파일"** 역할입니다.
예를 들어:
- 1495명 처리하다가 중간에 컴퓨터가 꺼지거나, 인터넷이 끊기거나, 잘못해서 프로그램을 종료해도
- 다음에 같은 엑셀 파일·같은 행 범위(`1~1495`)로 다시 실행하면
- BACKUP 파일을 자동으로 읽어서 **이미 처리한 환자는 건너뛰고**, 안 한 환자부터 이어서 진행합니다.
- 콘솔에 이런 메시지가 뜨면 정상 작동 중인 것:
  > 🔄 백업에서 800명의 기존 결과를 로드했습니다. 나머지부터 이어서 진행합니다.

### 삭제해도 되나요?
- ✅ **작업이 정상적으로 끝까지 완료되었다면** → 프로그램이 **자동으로 삭제**합니다. 신경 쓸 필요 없음.
  - 콘솔에 `🗑 백업 파일 삭제 완료`가 뜨면 자동 삭제됨.
  - `RESULT_...xlsx` 파일이 만들어졌다면 BACKUP은 필요 없습니다.
- ⚠️ **작업이 중간에 멈췄거나, 컴퓨터가 꺼져서 BACKUP만 남았다면** → **절대 삭제하지 마세요.**
  - 이 파일이 있어야 다음 실행 때 처음부터 다시 시작하지 않습니다.
- 🤔 **이미 끝난 작업의 BACKUP 파일이 남아 있다면?** → 삭제해도 됩니다.
  - 단, 같은 행 범위로 다시 실행하면 "이미 완료" 처리되어 새로 안 돌아갈 수 있습니다.

### 자주 묻는 질문
- **Q. BACKUP 파일을 열어봤더니 결과가 모자라요. 왜?**
  A. 정상입니다. 처리 중에 마지막으로 저장된 시점까지의 결과만 들어있는 것입니다.

- **Q. BACKUP 파일이 두 개 이상 생겼어요!**
  A. 행 범위(시작행~끝행)가 다르면 다른 BACKUP 파일이 만들어집니다.
  예: `BACKUP_파일이름_1_500.xlsx`와 `BACKUP_파일이름_501_1000.xlsx`는 서로 다른 작업.

- **Q. BACKUP 파일을 수동으로 수정해도 되나요?**
  A. **권장하지 않습니다.** `_original_index`와 `Probability_Result` 컬럼이 정확히 맞아야 resume 기능이 작동합니다.

---

## 2️⃣ RESULT 파일 — 최종 결과

### 파일 이름 예시
```
RESULT_강남septic shock_대상자_0511_수집중_1_1495_20260513-153022.xlsx
       └─ 원본 엑셀 이름 ───────────────────┘ └┬┘ └┬─┘ └─────┬─────┘
                                            시작행 끝행 작업 완료 시각
```

### 언제 만들어지나요?
- 지정한 범위의 환자를 **전부 처리하고 작업이 정상 종료**되면 1번 만들어집니다.
- 파일명에 **작업 완료 시각(날짜-시간)**이 붙어서, 여러 번 돌려도 덮어써지지 않습니다.

### 무엇이 들어 있나요?
- 원본 엑셀의 모든 컬럼 + 맨 끝에 `Probability_Result` 컬럼 추가
- 이 컬럼에 사이트가 예측한 확률값(예: `45.3 %`)이 들어 있습니다.

### 보관해야 하나요?
- ✅ **네, 이게 진짜 결과 파일입니다.** 안전한 곳에 보관하세요.

---

## 3️⃣ NA 파일 — 실패 환자 목록 (조건부 생성)

### 파일 이름 예시
```
NA_강남septic shock_대상자_0511_수집중_1_1495_20260513-153022.xlsx
```

### 언제 만들어지나요?
- 작업 완료 후, **결과를 못 가져온 환자(N/A)가 1명이라도 있을 때만** 만들어집니다.
- 모든 환자의 결과가 정상이라면 이 파일은 **생성되지 않습니다.**

### 무엇이 들어 있나요?
- 결과가 `N/A`로 나온 환자들의 **원본 데이터만** 따로 모은 파일입니다.
- 사이트 접속 실패, 시간 초과, 데이터 입력 오류 등으로 결과를 못 받은 경우.

### 어떻게 활용하나요?
- 이 NA 파일을 그대로 프로그램 입력으로 다시 넣으면, **실패한 환자만 재처리**할 수 있습니다.
- 재처리 후에는 결과 파일을 따로 보관하면 됩니다.

### 보관해야 하나요?
- ✅ **재처리 전에는 보관**하세요.
- 재처리해서 모두 정상 결과를 얻었다면 삭제해도 됩니다.

---

## 🧹 폴더 정리 가이드

작업이 끝난 후 폴더에는 보통 다음과 같이 남게 됩니다:

```
script_enroll/
├── 원본엑셀.xlsx                                      ← 입력 파일 (보관)
├── RESULT_원본엑셀_1_1495_20260513-153022.xlsx       ← 최종 결과 ✅ 보관
├── NA_원본엑셀_1_1495_20260513-153022.xlsx           ← N/A 환자만 (있을 때만) ✅ 보관
└── (BACKUP 파일은 자동 삭제됨)
```

### 정리해도 되는 파일
- ✅ 작업 끝난 후 남아 있는 **오래된 BACKUP 파일** (같은 행 범위로 재실행 안 할 거라면)
- ✅ 이미 재처리 끝난 **오래된 NA 파일**

### 절대 건드리면 안 되는 파일
- ❌ **작업 중에 있는 BACKUP 파일** (프로그램 실행 중에는 절대 삭제·이동·열기 금지)
- ❌ 보관해야 할 **RESULT 파일**

---

## ⚠️ 실수 방지 팁

1. **프로그램이 돌아가는 중**에는 BACKUP 파일을 엑셀로 열지 마세요.
   → 엑셀이 파일을 잠궈서 저장에 실패하고 결과가 날아갈 수 있습니다.

2. 같은 엑셀로 **다시 처음부터** 돌리고 싶다면, 먼저 해당 BACKUP 파일을 삭제하세요.
   → 안 그러면 "이미 완료" 처리되어 그냥 끝나버립니다.

3. **행 범위를 다르게** 지정하면 BACKUP 파일이 따로 생깁니다.
   → `1~500` 작업의 BACKUP과 `501~1000` 작업의 BACKUP은 별개.

4. 파일 이름의 **숫자(`_1_1495`)는 행 범위**입니다. 작업 식별에 활용하세요.
